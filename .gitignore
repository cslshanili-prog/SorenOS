# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.npm-cache
dist
dist-ssr
*.local

# build artifact — `pnpm run build:workers` 原樣複製過來的，與源文件逐字節相同；
# 提交的那份在 worker/amsg/deno-proxy.ts
public/amsg-deno-proxy.ts

# build artifact — 每次 `pnpm run build` 都會重新打，跟著提交只會讓跑完構建的人
# 平白多一條 diff。提交的那份在 worker/amsg/worker.bundle.js（wrangler.toml 的
# main 指著它，手動 wrangler deploy 要用）。
#
# 發佈鏈路都會自己生成，不會因為倉庫裡沒有它就少東西：
#   - GitHub Pages：deploy-pages.yml 跑 `pnpm run build`
#   - Netlify：本地構建後上傳 dist
#   - 用戶自部署的 worker 倉庫：sync-workers-repo.yml 跑 `pnpm build:workers`
public/amsg-worker.bundle.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# xiaohongshu-skills (cloned dependency)
xiaohongshu-skills/

# wrangler 本地開發：.dev.vars 裝的是 worker secrets（master key / VAPID 私鑰），
# .wrangler/ 是本地 D1 數據與緩存，兩樣都不進倉庫。
**/.dev.vars
**/.dev.vars.*
!**/.dev.vars.example
**/.wrangler/

# git worktrees
.worktrees/
.claude/
.pnpm-store/

# TIAO-Capacitor local Android toolchain and caches (D: only, never committed)
.android-sdk/
.gradle-user-home/

# Local fishing QA screenshots, cached existing Tailwind CDN script, isolated build.
/output/fishing-qa/
/output/fishing-build/

# Local PNG share card QA and isolated build.
/output/png-share-qa/
/output/png-share-build/

# Local SAR Three.js render QA and isolated production build.
/output/sar-diorama-qa/
/output/sar-diorama-build/
/output/sar-diorama-*.log
/output/dino-cafe-qa/
/output/dino-cafe-build/

/output/dino-garden-qa/
/output/dino-garden-build/
/output/sar-art-qa/
/output/sar-art-build/

# Local anniversary browser QA and build output.
/output/anniversary-qa/
/output/anniversary-build/

# Local generated previews, browser QA and isolated build artifacts.
/output/
