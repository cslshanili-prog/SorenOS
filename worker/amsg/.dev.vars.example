# 主動消息 2.0 後端要的密鑰。
#
# 走「Deploy to Cloudflare」按鈕部署時，這裡列出的每一項都會變成部署頁面上的一個輸入框，
# 旁邊的說明來自 package.json 的 cloudflare.bindings。值都從 SullyOS 的設置頁裡複製。
#
# 本地開發的話，把這個文件複製成 .dev.vars 再填。

AMSG_MASTER_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=
AMSG_SERVER_TOKEN=
